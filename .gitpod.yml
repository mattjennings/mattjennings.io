tasks:
  - init: |
      npm install -g pnpm
      pnpm install
    before: |
      npm install -g pnpm
      echo 'export GITPOD_URL="$(gp url 3000)"' >> $HOME/.bashrc
    command: pnpm run dev

ports:
  - port: 3000
    onOpen: open-preview